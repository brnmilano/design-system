import { Meta } from '@storybook/addon-docs'
import { fontWeights } from '@brnmilano-ui/tokens'
import TokensGrid from '../../components/TokensGrid'

<Meta title="Tokens/Font Weights"/>

# Font Weights

Os tokens de Font Weights são usados para definir o peso das fontes da aplicação.
Eles são usados como font-weight, em textos, títulos, etc.

<TokensGrid tokens={fontWeights} />