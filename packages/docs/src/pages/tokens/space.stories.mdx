import { Meta } from '@storybook/addon-docs'

import { space } from '@brnmilano-ui/tokens'
import TokensGrid from '../../components/TokensGrid'

<Meta title="Tokens/Spaces"/>

# Space

Os tokens de espaçamento são usados ​​para definir o espaço entre os elementos.
Eles são usados como padding, margin, top, bottom, left, right, etc.

<TokensGrid tokens={space} hasRemValue />