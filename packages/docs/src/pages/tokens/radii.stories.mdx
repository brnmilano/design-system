import { Meta } from '@storybook/addon-docs'
import { radii } from '@brnmilano-ui/tokens'
import TokensGrid from '../../components/TokensGrid'

<Meta title="Tokens/Radii"/>

# Radii

Os tokens de Radii são usados para definir o raio das bordas dos elementos da aplicação.
Eles são usados como border-radius, em botões, cards, etc.

<TokensGrid tokens={radii} />