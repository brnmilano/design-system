import { Meta } from '@storybook/addon-docs'
import { fontSizes } from '@brnmilano-ui/tokens'
import TokensGrid from '../../components/TokensGrid'

<Meta title="Tokens/Font Sizes"/>

# Font Sizes

Os tokens de Font Size são usados ​​para definir o tamanho das fontes da aplicação.
Eles são usados como font-size, em textos, títulos, etc.

<TokensGrid tokens={fontSizes} hasRemValue />